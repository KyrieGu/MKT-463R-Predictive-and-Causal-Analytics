#INSERT CODE HERE
#read the data
df <-read.csv("Homework 1 Data - 436R.csv")
#peek
names(df)
summary(df)
#INSERT CODE HERE
head(df,10)
#INSERT CODE HERE
earliest <- subset(df,min)
#INSERT CODE HERE
earliest <- subset(df,date = min(date))
earliset
#INSERT CODE HERE
subset(df,date = min(date))
#INSERT CODE HERE
subset(df,date == min(date) & isTreatmentPeriod == 1)
#INSERT CODE HERE
subset(df,date == min(date) & isTreatmentPeriod == 1)
#INSERT CODE HERE
#read the data
df <-read.csv("Homework 1 Data - 436R.csv")
#peek
names(df)
summary(df)
#INSERT CODE HERE
head(df,10)
#INSERT CODE HERE
subset(df,date == min(date) & isTreatmentPeriod == 1)
View(df)
#INSERT CODE HERE
trea_dates <- subset(df,isTreatmentPeriod == 1)
#INSERT CODE HERE
treat_dates <- subset(df,isTreatmentPeriod == 1)
head(treat_dates)
treat<-subset(treat_dates, date == min(date))
treat
View(df)
#INSERT CODE HERE
treat_dates <- subset(df,isTreatmentPeriod == 1)
#INSERT CODE HERE
#read the data
df <-read.csv("Homework 1 Data - 436R.csv")
#peek
names(df)
summary(df)
#INSERT CODE HERE
head(df,10)
#INSERT CODE HERE
treat_dates <- subset(df,isTreatmentPeriod == 1)
head(treat_dates)
min(treat_dates$date)
#INSERT CODE HERE
treat_dates <- subset(df,isTreatmentPeriod == 1)
min(treat_dates$date)
#INSERT CODE HERE
treatment_group <- subset(df,isTreatmentGroup == 1)
#ii.
lm(revenue~isTreatmentPeriod,data = treatment_group)
#ii.
lm((logrevenue)~isTreatmentPeriod,data = treatment_group)
#ii.
lm((log(revenue)~isTreatmentPeriod,data = treatment_group)
#INSERT CODE HERE
#i.
treatment_group <- subset(df,isTreatmentGroup == 1)
#ii.
#ii.
lm((log(revenue)~isTreatmentPeriod,data = treatment_group)
#INSERT CODE HERE
#i.
treatment_group <- subset(df,isTreatmentGroup == 1)
#ii.
lm(log(revenue)~isTreatmentPeriod,data = treatment_group)
#INSERT CODE HERE
#i. subset treatment group
treatment_group <- subset(df,isTreatmentGroup == 1)
#ii. log(revenue)
lm(log(revenue)~isTreatmentPeriod,data = treatment_group)
#iii. summary
summary(lm(log(revenue)~isTreatmentPeriod,data = treatment_group))
#INSERT CODE HERE
#i. subset data from the pre treatment period
pre_treat <- subset(treatment_group, isTreatmentPeriod == 0)
#ii.
lm(log(revenue)~isTreatmentGroup,data = pre_treat)
View(pre_treat)
View(pre_treat)
#INSERT CODE HERE
#i. subset data from the pre treatment period
pre_treat <- subset(df, isTreatmentPeriod == 0)
#ii.
lm(log(revenue)~isTreatmentGroup,data = pre_treat)
#INSERT CODE HERE
#i. subset data from the pre treatment period
pre_treat <- subset(df, isTreatmentPeriod == 0)
#ii. log(revenue) ~ treatment group
lm(log(revenue)~isTreatmentGroup,data = pre_treat)
#iii. summary
summary(lm(log(revenue)~isTreatmentGroup,data = pre_treat))
#INSERT CODE HERE
#subset all started period
post_treat <- subset(df,isTreatmentPeriod == 1)
#find the earliest date
min(treat_dates$date)
#INSERT CODE HERE
#subset all started period
post_treat <- subset(df,isTreatmentPeriod == 1)
#find the earliest date
min(post_treat$date)
#INSERT CODE HERE
summary(lm(log(revenue) ~ isTreatmentGroup, data = post_treat))
#INSERT CODE HERE
#i. subset data from the pre treatment period
pre_treat <- subset(df, isTreatmentPeriod == 0)
#ii. log(revenue) ~ treatment group
lm(log(revenue)~isTreatmentGroup,data = pre_treat)
#iii. summary
summary(lm(log(revenue)~isTreatmentGroup,data = pre_treat))
#INSERT CODE HERE
#i. subset treatment group
treatment_group <- subset(df,isTreatmentGroup == 1)
#ii. log(revenue) ~ treatment period
lm(log(revenue)~isTreatmentPeriod,data = treatment_group)
#iii. summary
summary(lm(log(revenue)~isTreatmentPeriod,data = treatment_group))
#INSERT CODE HERE
#log(revenue) ~ treatment group from post-treatment period
summary(lm(log(revenue) ~ isTreatmentGroup, data = post_treat))
#INSERT CODE HERE
#read the data
df <-read.csv("Homework 1 Data - 436R.csv")
#peek
names(df)
summary(df)
#INSERT CODE HERE
#top 10 data
head(df,10)
#INSERT CODE HERE
#subset all started period
post_treat <- subset(df,isTreatmentPeriod == 1)
#find the earliest date
min(post_treat$date)
#INSERT CODE HERE
#i. subset treatment group
treatment_group <- subset(df,isTreatmentGroup == 1)
#ii. log(revenue) ~ treatment period
lm(log(revenue)~isTreatmentPeriod,data = treatment_group)
#iii. summary
summary(lm(log(revenue)~isTreatmentPeriod,data = treatment_group))
#INSERT CODE HERE
#i. subset data from the pre treatment period
pre_treat <- subset(df, isTreatmentPeriod == 0)
#ii. log(revenue) ~ treatment group
lm(log(revenue)~isTreatmentGroup,data = pre_treat)
#iii. summary
summary(lm(log(revenue)~isTreatmentGroup,data = pre_treat))
#INSERT CODE HERE
#log(revenue) ~ treatment group from post-treatment period
summary(lm(log(revenue) ~ isTreatmentGroup, data = post_treat))
